{
  "name": "claude-pokemon-cli",
  "private": true,
  "type": "module",
  "dependencies": {
    "claude-pokemon-shared": "*"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "typescript": "^5.5.0",
    "vitest": "^4.1.5"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:engine": "npm run build && node scripts/build-engine.mjs",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepare": "tsc -p tsconfig.json"
  }
}
