node_modules/
*.tgz
.DS_Store
# dist/ is COMMITTED so consumers (worker workspaces, arena submodule) get
# pre-built JS+d.ts without running a build pipeline. Whoever edits src/
# MUST run `npm run build` and commit the regenerated dist/. The
# build-drift CI check in claude-pokemon root catches stale dist.
